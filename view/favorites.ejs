<!DOCTYPE html>
<html lang="nl">
    
<%- include ('../view/partials/head') %>

<body>
<%- include ('../view/partials/header') %>

<!-- het dier wordt weergegevn wat er bij past -->
<main>
    <div class="results">
    <% favorites.forEach((favorite) => { %>
    <div class="info"><img class="petpicture" src=" <%= favorite.photo %>" alt="photo of a pet" />
       <div><h2><%= favorite.name %></h2>
            <h2><%= favorite.age %></h2>
            <h2><%= favorite.soort %></h2>
            <h2><%= favorite.trait %></h2>
            <form class="favoriteForm" method="post" action="">
                <button name="favorite" type="submit" value="<%= pet.name %>">Remove</button>
            </form></div>
    <% }) %>

    <a href="http://localhost:4000/"><button>Retake!</button></a>
    </div>
    </main>
    <script src="../index.js"></script>

</body>
</html>
